import React from 'react'
import Head from 'next/head'
import { buildFaqJsonLd, buildHowToJsonLd } from '../lib/schema'
import Layout from '../components/Layout'
import { PRODUCT } from '../lib/product'
import { geoPosts } from '../data/geoPosts'

const SHRM = 'https://www.shrm.org/'
const GALLUP = 'https://www.gallup.com/'
const HBR = 'https://hbr.org/'

const posts = [
  {
    slug: 'what-is-30-60-90-onboarding-plan',
    title: 'What is a 30/60/90 day onboarding plan?',
    type: 'Definitional · FAQPage',
    query: 'what is a 30 60 90 onboarding plan',
    body: "A 30/60/90 onboarding plan splits a new hire's first quarter into three windows — learn by day 30, do by day 60, own by day 90 — so expectations, goals, and check-ins are explicit per role. It is a planning aid, not a guarantee of retention or success.",
    refs: [SHRM, GALLUP],
  },
  {
    slug: 'how-to-build-30-60-90-onboarding-plan',
    title: 'How to build a 30/60/90 day onboarding plan by role',
    type: 'How-to · HowTo',
    query: 'build 30 60 90 onboarding plan by role',
    body: 'Name the role, seniority, and work mode; set observable goals for each window; assign 2–3 check-ins with owners; add a first-week checklist. OnboardPlan generates the draft from those three inputs so the manager edits instead of starting blank.',
    refs: [SHRM, GALLUP],
  },
  {
    slug: '30-60-90-plan-vs-generic-checklist',
    title: '30/60/90 plan vs a generic onboarding checklist',
    type: 'Comparison',
    query: '30 60 90 plan vs checklist',
    body: 'A checklist lists tasks; a 30/60/90 plan sequences them into learn/do/own windows with role-tuned goals and named check-ins. They solve different problems and work best together — the plan attaches a first-week checklist for logistics.',
    refs: [SHRM, HBR],
  },
  {
    slug: '30-60-90-onboarding-plan-use-cases',
    title: 'Who should use a 30/60/90 day onboarding plan',
    type: 'Use cases',
    query: '30 60 90 onboarding plan use cases',
    body: 'New managers, scale-ups hiring repeat roles, remote teams, and HR generalists standardizing onboarding benefit most. The format is sharpest for functions with a clear, measurable ramp like sales, customer success, and support.',
    refs: [SHRM, GALLUP],
  },
  {
    slug: 'common-30-60-90-onboarding-mistakes',
    title: 'Common 30/60/90 onboarding plan mistakes',
    type: 'How-to · HowTo',
    query: 'common 30 60 90 onboarding mistakes',
    body: 'The usual errors: vague goals, unnamed check-ins, one plan for every level, ignoring work mode, and writing the plan once and forgetting it. Each is fixable by making outcomes observable and the calendar explicit.',
    refs: [SHRM, HBR],
  },
] as {
  slug: string
  title: string
  type: string
  query: string
  body: string
  refs: string[]
}[]

const faqs = [
  {
    "question": "What is a 30/60/90 day onboarding plan?",
    "answer": "It splits a new hire's first 90 days into three windows — learn by day 30, do by day 60, own by day 90 — with explicit goals and check-ins per role. It is a planning aid, not a guarantee of retention or success."
  },
  {
    "question": "Does OnboardPlan guarantee new hires will be retained?",
    "answer": "No. OnboardPlan is a planning aid that makes the first 90 days explicit and editable. It does not guarantee retention or onboarding success; those depend on the manager, the team, the role, and the individual."
  },
  {
    "question": "Who owns the onboarding plan — the manager or HR?",
    "answer": "The manager usually owns the day-to-day plan and check-ins; HR often owns the process and templates. OnboardPlan produces a role-based draft the manager can edit, so ownership stays with the hiring manager."
  },
  {
    "question": "Is a 30/60/90 plan only for sales roles?",
    "answer": "No. The format works for any function with a defined ramp — engineering, customer success, marketing, ops, support. The plan is role-tuned, so expectations differ by role and seniority."
  },
  {
    "question": "How much does OnboardPlan cost?",
    "answer": "Free is $0 for 4 plans per month. Pro is $15/mo for unlimited plans and saved history; annual Pro is $150/year. These match the published tiers — confirm the current plan on the live site."
  }
] as { question: string; answer: string }[]

const howToBlocks = [
  {
    "name": "How to build a 30/60/90 day onboarding plan by role",
    "steps": [
      {
        "name": "Overview",
        "text": "Name the role, seniority, and work mode; set observable goals for each window; assign 2–3 check-ins with owners; add a first-week checklist. OnboardPlan generates the draft from those three inputs so the manager edits instead of starting blank."
      }
    ]
  },
  {
    "name": "Common 30/60/90 onboarding plan mistakes",
    "steps": [
      {
        "name": "Overview",
        "text": "Avoid vague goals, unnamed check-ins, one plan for every level, ignoring work mode, and writing the plan once. Make outcomes observable and the check-in calendar explicit so gaps surface early."
      }
    ]
  },
  {
    "name": "OnboardPlan tool guide: inputs, features, and pricing",
    "steps": [
      {
        "name": "Overview",
        "text": "OnboardPlan takes role, seniority, and work mode and returns a role-tuned 30/60/90 plan with check-ins, owners, and a first-week checklist. Free is $0 (4 plans/mo); Pro is $15/mo (unlimited, saved history)."
      }
    ]
  }
] as { name: string; steps: { name: string; text: string }[] }[]

export default function BlogPage() {
  return (
    <Layout>
      <Head>
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{ __html: JSON.stringify(buildFaqJsonLd(faqs)) }}
        />
        {howToBlocks.map((block, i) => (
          <script
            key={`howto-${i}`}
            type="application/ld+json"
            dangerouslySetInnerHTML={{
              __html: JSON.stringify(buildHowToJsonLd(block.name, block.steps)),
            }}
          />
        ))}

        <title>{`${PRODUCT.name} — Blog`}</title>
        <meta name="description" content="Definitional and how-to posts on 30/60/90 onboarding plans — what the three windows mean, how to build one by role, common mistakes, and the use cases teams ask about before they onboard a new hire." />
      </Head>
      <div className="max-w-3xl">
        <div className="text-xs font-bold tracking-widest uppercase text-indigo-600 mb-3">Blog · GEO</div>
        <h1 className="text-4xl font-extrabold tracking-tight mb-4">30/60/90 onboarding plans, explained</h1>
        <p className="text-lg text-slate-600 mb-10">Own the definitional queries teams ask before they onboard a new hire — what the windows mean, how to build a plan by role, and where the format fits.</p>

        <div className="space-y-8">
          {posts.map((p) => (
            <article key={p.slug} className="border-b border-slate-200 pb-8">
              <div className="text-xs font-semibold text-indigo-600 mb-1">{p.type}</div>
              <h2 className="text-2xl font-bold mb-2 text-slate-900">{p.title}</h2>
              <p className="text-sm text-slate-600 mb-2"><span className="font-semibold">Target query:</span> {p.query}</p>
              <p className="text-slate-700 leading-relaxed">{p.body}</p>
              <p className="text-xs text-slate-400 mt-3">refs: {p.refs.join(' · ')}</p>
            </article>
          ))}
        </div>

        <h2 className="text-2xl font-bold mt-14 mb-2 text-slate-900">Deep-dives (GEO)</h2>
        <p className="text-sm text-slate-500 mb-6">Long-form explainers on 30/60/90 onboarding plans. Each carries an FAQ and a planning-aid disclaimer — no guarantees of retention or success.</p>
        <div className="space-y-5">
          {geoPosts.map((p) => (
            <div key={p.slug} className="border-b border-slate-200 pb-5">
              <h3 className="text-xl font-semibold">
                <a href={`/blog/${p.slug}`} className="text-indigo-700 hover:underline">{p.title}</a>
              </h3>
              <p className="text-sm text-slate-600 mt-1">{p.description}</p>
            </div>
          ))}
        </div>

        <p className="text-xs text-slate-400 mt-8">Publish + syndicate per gtm-launch (IH + GEO indexes). Each post carries an FAQ and a planning-aid disclaimer.</p>
      </div>
    </Layout>
  )
}
